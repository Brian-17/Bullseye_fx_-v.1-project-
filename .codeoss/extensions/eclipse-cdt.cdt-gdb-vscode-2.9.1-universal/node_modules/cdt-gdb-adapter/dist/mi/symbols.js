"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendSymbolInfoVars = sendSymbolInfoVars;
exports.sendSymbolInfoFunctions = sendSymbolInfoFunctions;
exports.sendSymbolListLines = sendSymbolListLines;
const standardEscape_1 = require("../util/standardEscape");
function sendSymbolInfoVars(gdb, params) {
    let command = '-symbol-info-variables';
    if (params) {
        if (params.name) {
            command += ` --name ${params.name}`;
        }
        if (params.type) {
            command += ` --type ${params.type}`;
        }
        if (params.max_result) {
            command += ` --max-result ${params.max_result}`;
        }
        if (params.non_debug) {
            command += ' --include-nondebug';
        }
    }
    return gdb.sendCommand(command);
}
function sendSymbolInfoFunctions(gdb, params) {
    let command = '-symbol-info-functions';
    if (params) {
        if (params.name) {
            command += ` --name ${params.name}`;
        }
        if (params.type) {
            command += ` --type ${params.type}`;
        }
        if (params.max_result) {
            command += ` --max-result ${params.max_result}`;
        }
        if (params.non_debug) {
            command += ' --include-nondebug';
        }
    }
    return gdb.sendCommand(command);
}
function sendSymbolListLines(gdb, file) {
    return __awaiter(this, void 0, void 0, function* () {
        const response = yield gdb.sendCommand(`-symbol-list-lines ${(0, standardEscape_1.standardEscape)(file)}`);
        return response;
    });
}
//# sourceMappingURL=symbols.js.map