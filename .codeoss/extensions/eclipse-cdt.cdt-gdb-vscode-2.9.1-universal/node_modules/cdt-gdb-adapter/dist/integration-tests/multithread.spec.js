"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
const chai_1 = require("chai");
const path = require("path");
const assert_1 = require("assert");
const os = require("os");
describe('multithread', function () {
    return __awaiter(this, void 0, void 0, function* () {
        let dc;
        const program = path.join(utils_1.testProgramsDir, 'MultiThread');
        const source = path.join(utils_1.testProgramsDir, 'MultiThread.cc');
        const threadNames = {
            monday: 0,
            tuesday: 1,
            wednesday: 2,
            thursday: 3,
            friday: 4,
        };
        const lineTags = {
            LINE_MAIN_ALL_THREADS_STARTED: 0,
            LINE_THREAD_IN_HELLO: 0,
            LINE_THREAD_INNER: 0,
        };
        before(function () {
            (0, utils_1.resolveLineTagLocations)(source, lineTags);
        });
        beforeEach(() => __awaiter(this, void 0, void 0, function* () {
            dc = yield (0, utils_1.standardBeforeEach)();
        }));
        afterEach(() => __awaiter(this, void 0, void 0, function* () {
            yield dc.stop();
        }));
        it('sees all threads', function () {
            return __awaiter(this, void 0, void 0, function* () {
                if (!utils_1.gdbNonStop && os.platform() === 'win32' && utils_1.isRemoteTest) {
                    // The way thread names are set in remote tests on windows is unsupported
                    this.skip();
                }
                yield dc.hitBreakpoint((0, utils_1.fillDefaults)(this.test, {
                    program: program,
                }), {
                    path: source,
                    line: lineTags['LINE_MAIN_ALL_THREADS_STARTED'],
                });
                const threads = yield dc.threadsRequest();
                const nameToId = new Map(threads.body.threads.map((thread) => [thread.name, thread.id]));
                // Make sure all 5 threads are there
                (0, chai_1.expect)(nameToId).to.include.keys(Object.keys(threadNames));
                // and make sure that there is at least 6 threads.
                // We don't care about the name of the "main" thread
                (0, chai_1.expect)(threads.body.threads).length.greaterThanOrEqual(6);
                // check that each thread can be communicated with individually
                for (const [name, idInProgram] of Object.entries(threadNames)) {
                    // There are multiple ids/indexes.
                    // idInProgram cooresponds to the variable thread_id in the C++ source
                    // threadId is the id of the thread in DAP
                    const threadId = nameToId.get(name);
                    if (threadId === undefined) {
                        // unreachable because of expect above
                        (0, assert_1.fail)('unreachable');
                    }
                    if (utils_1.gdbNonStop) {
                        const waitForStopped = dc.waitForEvent('stopped');
                        const pr = dc.pauseRequest({ threadId });
                        yield Promise.all([pr, waitForStopped]);
                    }
                    const stack = yield dc.stackTraceRequest({ threadId });
                    let printHelloFrameId = undefined;
                    let callerFrameId = undefined;
                    for (const frame of stack.body.stackFrames) {
                        if (frame.name === 'PrintHello') {
                            printHelloFrameId = frame.id;
                        }
                        else if (printHelloFrameId !== undefined) {
                            callerFrameId = frame.id;
                            break;
                        }
                    }
                    if (printHelloFrameId === undefined) {
                        (0, assert_1.fail)("Failed to find frame with name 'PrintHello'");
                    }
                    if (callerFrameId === undefined) {
                        (0, assert_1.fail)("Failed to find frame that called 'PrintHello'");
                    }
                    {
                        const scopes = yield dc.scopesRequest({
                            frameId: callerFrameId,
                        });
                        const vr = scopes.body.scopes[0].variablesReference;
                        const vars = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        const varnameToValue = new Map(vars.body.variables.map((variable) => [
                            variable.name,
                            variable.value,
                        ]));
                        // Make sure we aren't getting the HelloWorld frame's variables.
                        // The calling method (in glibc or similar) may end up with a local
                        // variable called thread_id, if so, update this heuristic
                        (0, chai_1.expect)(varnameToValue.get('thread_id')).to.be.undefined;
                    }
                    {
                        const scopes = yield dc.scopesRequest({
                            frameId: printHelloFrameId,
                        });
                        const vr = scopes.body.scopes[0].variablesReference;
                        const vars = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        const varnameToValue = new Map(vars.body.variables.map((variable) => [
                            variable.name,
                            variable.value,
                        ]));
                        (0, chai_1.expect)(varnameToValue.get('thread_id')).to.equal(idInProgram.toString());
                        // The "name" variable is a pointer, so is displayed as an address + the
                        // extracted nul terminated string
                        (0, chai_1.expect)(varnameToValue.get('name')).to.contain(name);
                    }
                    {
                        // Depending on which frame the top of the stack is,
                        // make sure we are getting the expected variables for that
                        // frame.
                        // This is a test for https://github.com/eclipse-cdt-cloud/cdt-gdb-adapter/issues/235
                        const topOfStackId = stack.body.stackFrames[0].id;
                        const topOfStackName = stack.body.stackFrames[0].name;
                        const scopes = yield dc.scopesRequest({
                            frameId: topOfStackId,
                        });
                        const vr = scopes.body.scopes[0].variablesReference;
                        const vars = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        const varnameToValue = new Map(vars.body.variables.map((variable) => [
                            variable.name,
                            variable.value,
                        ]));
                        // Check that thread_id is the correct value and that one other
                        // local variable in the method exists.
                        // We don't check local variable values because it may not be
                        // correct yet, for example if the program stopped before assigning
                        // the value to thread_id_plus_1 testing it against thread_id + 1
                        // would lead to rare, but intermittent, test failures.
                        if (topOfStackName === 'inner_method') {
                            (0, chai_1.expect)(varnameToValue.get('thread_id')).to.equal(idInProgram.toString());
                            (0, chai_1.expect)(varnameToValue.get('thread_id_plus_1')).to.not.be
                                .undefined;
                        }
                        else if (topOfStackName === 'recursive') {
                            (0, chai_1.expect)(varnameToValue.get('thread_id')).to.equal(idInProgram.toString());
                            (0, chai_1.expect)(varnameToValue.get('depth')).to.not.be.undefined;
                        }
                        else if (topOfStackName === 'PrintHello') {
                            (0, chai_1.expect)(varnameToValue.get('thread_id')).to.equal(idInProgram.toString());
                            (0, chai_1.expect)(varnameToValue.get('void_arg')).to.not.be.undefined;
                        }
                        else {
                            // The top of the stack is probably (and normally) not
                            // in our code, but rather in the various semaphore code
                            // therefore make sure our "thread_id" is not visible
                            // It could happen that the sempahore (or related code) ends
                            // up with a local called "thread_id" in the future - if so,
                            // this will fail and needs to be updated
                            (0, chai_1.expect)(varnameToValue.get('thread_id')).to.be.undefined;
                        }
                    }
                }
            });
        });
        it('async resume for gdb-non-stop off', function () {
            return __awaiter(this, void 0, void 0, function* () {
                if (utils_1.gdbNonStop) {
                    // This test is covering only gdb-non-stop off mode
                    this.skip();
                }
                else if (os.platform() === 'win32' && (!utils_1.isRemoteTest || !utils_1.gdbAsync)) {
                    // Only supported in win32 host with remote + mi-async targets
                    this.skip();
                }
                yield dc.launchRequest((0, utils_1.fillDefaults)(this.test, {
                    program,
                }));
                yield dc.setBreakpointsRequest({
                    source: {
                        path: source,
                    },
                    breakpoints: [
                        {
                            line: lineTags['LINE_MAIN_ALL_THREADS_STARTED'],
                        },
                        {
                            line: lineTags['LINE_THREAD_IN_HELLO'],
                        },
                    ],
                });
                const waitForStop = dc.waitForEvent('stopped');
                yield dc.configurationDoneRequest();
                yield waitForStop;
                const threads = yield dc.threadsRequest();
                // make sure that there is at least 2 threads.
                (0, chai_1.expect)(threads.body.threads).length.greaterThanOrEqual(2);
                const waitForContinue = dc.waitForEvent('continued');
                // Send continue to thread 2
                dc.send('cdt-gdb-tests/executeCommand', {
                    command: '-exec-continue --thread 2',
                });
                const event = yield waitForContinue;
                // In allThreadsContinued:true case we are expecting id of the first thread no matter which thread is continued
                chai_1.assert.deepEqual(event.body, {
                    threadId: threads.body.threads[0].id,
                    allThreadsContinued: true,
                });
            });
        });
        it('async resume for gdb-non-stop on', function () {
            return __awaiter(this, void 0, void 0, function* () {
                if (!utils_1.gdbNonStop) {
                    // This test is covering only gdb-non-stop on
                    this.skip();
                }
                yield dc.hitBreakpoint((0, utils_1.fillDefaults)(this.test, {
                    program: program,
                }), {
                    path: source,
                    line: lineTags['LINE_MAIN_ALL_THREADS_STARTED'],
                });
                const threads = yield dc.threadsRequest();
                // make sure that there is at least 6 threads.
                (0, chai_1.expect)(threads.body.threads).length.greaterThanOrEqual(6);
                // stop the running threads
                const runningThreads = threads.body.threads.filter((t) => t.running);
                for (const thread of runningThreads) {
                    const waitForStop = dc.waitForEvent('stopped');
                    yield dc.pauseRequest({ threadId: thread.id });
                    yield waitForStop;
                }
                for (const thread of threads.body.threads) {
                    const waitForContinue = dc.waitForEvent('continued');
                    // Send an async continue request and wait for the continue event.
                    dc.send('cdt-gdb-tests/executeCommand', {
                        command: `-exec-continue --thread ${thread.id}`,
                    });
                    const event = yield waitForContinue;
                    chai_1.assert.deepEqual(event.body, {
                        threadId: thread.id,
                        allThreadsContinued: false,
                    });
                }
            });
        });
        it('gets varible values at different frames for different threads', function () {
            return __awaiter(this, void 0, void 0, function* () {
                if (!utils_1.gdbNonStop) {
                    // This test is covering only gdb-non-stop on
                    this.skip();
                }
                yield dc.launchRequest((0, utils_1.fillDefaults)(this.test, {
                    program,
                }));
                yield dc.setBreakpointsRequest({
                    source: {
                        path: source,
                    },
                    breakpoints: [
                        {
                            line: lineTags['LINE_MAIN_ALL_THREADS_STARTED'],
                        },
                        {
                            line: lineTags['LINE_THREAD_INNER'],
                        },
                    ],
                });
                const waitForStop = dc.waitForEvent('stopped');
                yield dc.configurationDoneRequest();
                yield waitForStop;
                // make sure that all the threads have stopped
                // TODO instead of a sleep, wait until all threads have stopped
                yield new Promise((f) => setTimeout(f, 1000));
                const threads = yield dc.threadsRequest();
                const runningThreads = threads.body.threads.filter((t) => t.running);
                (0, chai_1.expect)(runningThreads).to.be.an('array').that.is.empty;
                const nameToId = new Map(threads.body.threads.map((thread) => [thread.name, thread.id]));
                // Make sure all 5 threads are there
                (0, chai_1.expect)(nameToId).to.include.keys(Object.keys(threadNames));
                // and make sure that there is at least 6 threads.
                // We don't care about the name of the "main" thread
                (0, chai_1.expect)(threads.body.threads).length.greaterThanOrEqual(6);
                // check that each thread can be communicated with individually
                for (const [name, idInProgram] of Object.entries(threadNames)) {
                    // There are multiple ids/indexes.
                    // idInProgram cooresponds to the variable thread_id in the C++ source
                    // threadId is the id of the thread in DAP
                    const threadId = nameToId.get(name);
                    if (threadId === undefined) {
                        // unreachable because of expect above
                        (0, assert_1.fail)('unreachable');
                    }
                    const stack = yield dc.stackTraceRequest({ threadId });
                    // Iterate through stack frames:
                    // Stack frame 0 is the inner method
                    // Stack frames 1 to id + 1 are the recursive method
                    // Stack frame id + 2 is PrintHello
                    for (let i = 0; i < idInProgram + 3; i++) {
                        if (i == 0) {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('inner_method');
                        }
                        else if (i == idInProgram + 2) {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('PrintHello');
                        }
                        else {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('recursive');
                        }
                        const scopes = yield dc.scopesRequest({
                            frameId: stack.body.stackFrames[i].id,
                        });
                        const vr = scopes.body.scopes[0].variablesReference;
                        const vars = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        const varnameToValue = new Map(vars.body.variables.map((variable) => [
                            variable.name,
                            variable.value,
                        ]));
                        (0, chai_1.expect)(varnameToValue.get('thread_id')).to.equal(idInProgram.toString());
                        if (i == 0) {
                            (0, chai_1.expect)(varnameToValue.get('thread_id_plus_1')).to.equal((idInProgram + 1).toString());
                            (0, chai_1.expect)(varnameToValue.get('thread_id_plus_2')).to.equal((idInProgram + 2).toString());
                        }
                        if (i > 0 && i < idInProgram + 2) {
                            (0, chai_1.expect)(varnameToValue.get('depth')).to.equal((i - 1).toString());
                        }
                    }
                }
            });
        });
        it('gets and sets register values at different frames for different threads', function () {
            return __awaiter(this, void 0, void 0, function* () {
                if (!utils_1.gdbNonStop) {
                    // This test is covering only gdb-non-stop on
                    this.skip();
                }
                yield dc.launchRequest((0, utils_1.fillDefaults)(this.test, {
                    program,
                }));
                yield dc.setBreakpointsRequest({
                    source: {
                        path: source,
                    },
                    breakpoints: [
                        {
                            line: lineTags['LINE_MAIN_ALL_THREADS_STARTED'],
                        },
                        {
                            line: lineTags['LINE_THREAD_INNER'],
                        },
                    ],
                });
                const waitForStop = dc.waitForEvent('stopped');
                yield dc.configurationDoneRequest();
                yield waitForStop;
                // make sure that all the threads have stopped
                // TODO instead of a sleep, wait until all threads have stopped
                yield new Promise((f) => setTimeout(f, 1000));
                const threads = yield dc.threadsRequest();
                const runningThreads = threads.body.threads.filter((t) => t.running);
                (0, chai_1.expect)(runningThreads).to.be.an('array').that.is.empty;
                const nameToId = new Map(threads.body.threads.map((thread) => [thread.name, thread.id]));
                // check that each thread can be communicated with individually
                for (const [name, idInProgram] of Object.entries(threadNames)) {
                    // There are multiple ids/indexes.
                    // idInProgram cooresponds to the variable thread_id in the C++ source
                    // threadId is the id of the thread in DAP
                    const threadId = nameToId.get(name);
                    if (threadId === undefined) {
                        // unreachable because of expect above
                        (0, assert_1.fail)('unreachable');
                    }
                    const stack = yield dc.stackTraceRequest({ threadId });
                    // Iterate through stack frames:
                    // Stack frame 0 is the inner method
                    // Stack frames 1 to id + 1 are the recursive method
                    // Stack frame id + 2 is PrintHello
                    for (let i = 0; i < idInProgram + 3; i++) {
                        if (i == 0) {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('inner_method');
                        }
                        else if (i == idInProgram + 2) {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('PrintHello');
                        }
                        else {
                            (0, chai_1.expect)(stack.body.stackFrames[i].name).to.eq('recursive');
                        }
                        const scopes = yield dc.scopesRequest({
                            frameId: stack.body.stackFrames[i].id,
                        });
                        const vr = scopes.body.scopes[1].variablesReference;
                        const vars = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        const regPC = vars.body.variables.find((v) => v.name === 'pc' || v.name === 'rip');
                        // assert instead of expect to make tsc happy so we don't need forbidden non-null assertions below
                        (0, chai_1.assert)(regPC !== undefined, 'expected regPC to exist');
                        const reg0 = vars.body.variables[0];
                        const setRegPC = yield dc.setVariableRequest({
                            name: regPC.name,
                            value: '0x200',
                            variablesReference: vr,
                        });
                        (0, chai_1.expect)(setRegPC.body.value).to.equal('0x200');
                        const setReg0 = yield dc.setVariableRequest({
                            name: reg0.name,
                            value: '0x55555',
                            variablesReference: vr,
                        });
                        (0, chai_1.expect)(setReg0.body.value).to.equal('0x55555');
                        const vars1 = yield dc.variablesRequest({
                            variablesReference: vr,
                        });
                        (0, chai_1.expect)(vars1.body.variables.length).to.equal(vars.body.variables.length);
                        const varnameToValue1 = new Map(vars1.body.variables.map((variable) => [
                            variable.name,
                            variable.value,
                        ]));
                        (0, chai_1.expect)(varnameToValue1.get(regPC.name)).to.equal('0x200');
                        (0, chai_1.expect)(varnameToValue1.get(reg0.name)).to.equal('0x55555');
                        yield dc.setVariableRequest({
                            name: regPC.name,
                            value: regPC.value,
                            variablesReference: vr,
                        });
                        yield dc.setVariableRequest({
                            name: reg0.name,
                            value: reg0.value,
                            variablesReference: vr,
                        });
                    }
                }
            });
        });
    });
});
//# sourceMappingURL=multithread.spec.js.map