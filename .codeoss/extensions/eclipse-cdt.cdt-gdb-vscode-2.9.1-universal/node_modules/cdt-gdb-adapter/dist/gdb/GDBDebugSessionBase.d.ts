import * as mi from '../mi';
import { Handles, Logger, LoggingDebugSession } from '@vscode/debugadapter';
import { DebugProtocol } from '@vscode/debugprotocol';
import { Mutex } from 'async-mutex';
import { VarObjType } from '../varManager';
import { FrameReference, VariableReference, LaunchRequestArguments, AttachRequestArguments, MemoryResponse, FrameVariableReference, RegisterVariableReference, ObjectVariableReference, CDTDisassembleArguments, RequestArgRun } from '../types/session';
import { IGDBBackend, IGDBBackendFactory } from '../types/gdb';
import { ThreadWithStatus } from './common';
/**
 * Keeps track of where in the configuration phase (between initialized event
 * and configurationDone response) we are.
 */
declare const enum ConfiguringState {
    /** Configuration phase has not started yet. */
    INITIAL = 0,
    /** Configuration phase has started, target is running, no requests that
     * require pausing it have arrived yet. */
    CONFIGURING = 1,
    /** Configuration phase has started, at least one request that requires
     * pausing the target has arrived or it has been paused to begin with. */
    CONFIGURING_PAUSED = 2,
    /** Configuration phase is completed, the next unpausing is the one
     * associated with the end of the phase. */
    FINISHING = 3,
    /** Configuration phase is completed, any following unpausing corresponds
     * to a pausing outside of the configuration phase. */
    DONE = 4
}
interface PendingPauseRequest {
    threadId: number | undefined;
    resolveFunc?: (value: void | PromiseLike<void>) => void;
}
export declare function hexToBase64(hex: string): string;
export declare function base64ToHex(base64: string): string;
export declare abstract class GDBDebugSessionBase extends LoggingDebugSession {
    protected readonly backendFactory: IGDBBackendFactory;
    /**
     * Initial (aka default) configuration for launch/attach request
     * typically supplied with the --config command line argument.
     */
    protected static defaultRequestArguments?: any;
    /**
     * Frozen configuration for launch/attach request
     * typically supplied with the --config-frozen command line argument.
     */
    protected static frozenRequestArguments?: {
        request?: string;
    };
    protected gdb: IGDBBackend;
    protected auxGdb?: IGDBBackend;
    protected isBreakpointSent: boolean;
    protected isDataBreakpointSent: boolean;
    protected isFunctionBreakpointSent: boolean;
    protected isInstructionBreakpointSent: boolean;
    protected isAttach: boolean;
    /**
     * True if GDB is using the "remote" or "extended-remote" target. In
     * currently supported cases, always for type="gdbtarget"
     * (GDBTargetDebugSession) and never for type="gdb" (GDBDebugSession).
     */
    protected isRemote: boolean;
    protected isRunning: boolean;
    protected columnStartAt1: boolean;
    protected supportsRunInTerminalRequest: boolean;
    protected supportsMemoryReferences: boolean;
    protected supportsMemoryEvent: boolean;
    supportsGdbConsole: boolean;
    protected logger: Logger.Logger;
    protected frameHandles: Handles<FrameReference>;
    protected variableHandles: Handles<VariableReference>;
    protected functionBreakpoints: string[];
    protected logPointMessages: {
        [key: string]: string;
    };
    protected threads: ThreadWithStatus[];
    protected missingThreadNames: boolean;
    protected updateThreadInfo: 'missing' | 'when-requested' | 'never';
    protected runAfterConfiguration: RequestArgRun;
    /**
     * State variables for pauseIfNeeded/continueIfNeeded logic, mostly used for
     * temporary pause to insert breakpoints while the target is running.
     */
    protected waitPausedPromise?: Promise<void>;
    protected waitPaused?: (value?: void | PromiseLike<void>) => void;
    protected waitPausedPollTimer?: NodeJS.Timeout;
    protected waitPausedThreadId: number;
    protected waitPausedNeeded: boolean;
    protected pauseCount: number;
    protected breakpointMutex: Mutex;
    protected silentPauseRequests: PendingPauseRequest[];
    protected configuringState: ConfiguringState;
    protected isInitialized: boolean;
    protected deferredStopEvents: any[];
    protected firstContinueIsRun: boolean;
    /**
     *  customResetCommands from launch.json
     */
    protected customResetCommands?: string[];
    /**
     *  steppingResponseTimeout from launch.json
     */
    protected steppingResponseTimeout: number;
    constructor(backendFactory: IGDBBackendFactory);
    /**
     * Checks if debug adapter is connected and ready to process debug requests
     * driven by user interaction or other extensions.
     *
     * Note: The base class implementation is not restrictive and lets pass all
     * requests. This is technical debt caused by taking a shortcut when adding
     * session state management only for GDBTargetDebugSession. Yet, request
     * handlers in GDBDebugSessionBase have to be treated by this, too.
     * This has to be rectified in future in a separate PR.
     * @return true if standard debug operation requests can be processed, false
     * otherwise.
     */
    protected canRequestProceed(): boolean;
    /**
     * Checks if an error would bring extra value to the user and if it
     * should be reported in the debug adapter protocol response.
     * Note: Derived classes can override. They should call this base
     * implementation last.
     * @param error The error to check.
     * @return true if the error should be reported as a debug adapter protocol
     * error response, false otherwise.
     */
    protected shouldReportError(error: unknown): boolean;
    /**
     * Apply the initial and frozen launch/attach request arguments.
     * @param request the default request type to return if request type is not frozen
     * @param args the arguments from the user to apply initial and frozen arguments to.
     * @returns resolved request type and the resolved arguments
     */
    protected applyRequestArguments(request: 'launch' | 'attach', args: LaunchRequestArguments | AttachRequestArguments): ['launch' | 'attach', LaunchRequestArguments | AttachRequestArguments];
    /**
     * Handle requests not defined in the debug adapter protocol.
     */
    protected customRequest(command: string, response: DebugProtocol.Response, args: any): void;
    /**
     * Validate the launch/attach request arguments and throw if they contain
     * an invalid combination.
     * @param args the request arguments to validate.
     */
    protected validateRequestArguments(args: LaunchRequestArguments | AttachRequestArguments): void;
    /**
     * Apply the initial custom reset arguments.
     * @param args the arguments from the user to apply custom reset arguments to.
     */
    protected initializeSessionArguments(args: LaunchRequestArguments | AttachRequestArguments): void;
    /**
     * Implement the custom reset request.
     */
    protected customResetRequest(response: DebugProtocol.Response): void;
    protected getBreakpointModes(): DebugProtocol.BreakpointMode[] | undefined;
    protected initializeRequest(response: DebugProtocol.InitializeResponse, args: DebugProtocol.InitializeRequestArguments): void;
    protected getCapabilities(cap: DebugProtocol.Capabilities): void;
    private switchOutputToError;
    protected setupCommonLoggerAndBackends(args: LaunchRequestArguments | AttachRequestArguments, auxBackend?: boolean): Promise<void>;
    protected warnAsyncDisabled(): void;
    protected attachOrLaunchRequest(response: DebugProtocol.Response, request: 'launch' | 'attach', args: LaunchRequestArguments | AttachRequestArguments): Promise<void>;
    protected sendInitializedEvent(): void;
    protected attachRequest(response: DebugProtocol.AttachResponse, args: AttachRequestArguments): Promise<void>;
    protected launchRequest(response: DebugProtocol.LaunchResponse, args: LaunchRequestArguments): Promise<void>;
    protected spawn(args: LaunchRequestArguments | AttachRequestArguments, gdb?: IGDBBackend): Promise<void>;
    /**
     * Sends a pause command to GDBBackend, and resolves when the debugger is
     * actually paused. The paused thread ID is saved to `this.waitPausedThreadId`.
     * @param requireAsync - require gdb to be in async mode to pause
     */
    protected pauseIfNeeded(requireAsync?: false): Promise<void>;
    /**
     * Sends a pause command to GDBBackend, and resolves when the debugger is
     * actually paused. The paused thread ID is saved to `this.waitPausedThreadId`.
     *
     * @param requireAsync - require gdb to be in async mode to pause
     * @deprecated the `requireAsync` parameter should not be used and will be
     * removed in the future.
     * See {@link https://github.com/eclipse-cdt-cloud/cdt-gdb-adapter/pull/339#discussion_r1840549671}
     */
    protected pauseIfNeeded(requireAsync: true): Promise<void>;
    protected waitPausedDone(): void;
    protected setConfiguringStateDone(): void;
    protected continueIfNeeded(): Promise<void>;
    /**
     * Pause thread if running and suppress stopped event.
     * In contrast to pauseIfNeeded(), there is no expectation to resume after a debugger operation.
     *
     * Notes:
     *   - NOT to be called during configuration phase.
     *   - No requireAsync, supposed to be removed from pauseIfNeeded in future.
     */
    protected pauseIfRunning(): Promise<void>;
    protected completionsRequest(response: DebugProtocol.CompletionsResponse, args: DebugProtocol.CompletionsArguments): Promise<void>;
    protected dataBreakpointInfoRequest(response: DebugProtocol.DataBreakpointInfoResponse, args: DebugProtocol.DataBreakpointInfoArguments): Promise<void>;
    private getInstructionBreakpointList;
    private getWatchpointList;
    protected setDataBreakpointsRequest(response: DebugProtocol.SetDataBreakpointsResponse, args: DebugProtocol.SetDataBreakpointsArguments): Promise<void>;
    protected doSetDataBreakpointsRequest(response: DebugProtocol.SetDataBreakpointsResponse, args: DebugProtocol.SetDataBreakpointsArguments): Promise<void>;
    protected setInstructionBreakpointsRequest(response: DebugProtocol.SetInstructionBreakpointsResponse, args: DebugProtocol.SetInstructionBreakpointsArguments): Promise<void>;
    protected doSetInstructionBreakpointsRequest(response: DebugProtocol.SetInstructionBreakpointsResponse, args: DebugProtocol.SetInstructionBreakpointsArguments): Promise<void>;
    protected setBreakPointsRequest(response: DebugProtocol.SetBreakpointsResponse, args: DebugProtocol.SetBreakpointsArguments): Promise<void>;
    protected doSetBreakPointsRequest(response: DebugProtocol.SetBreakpointsResponse, args: DebugProtocol.SetBreakpointsArguments): Promise<void>;
    protected setFunctionBreakPointsRequest(response: DebugProtocol.SetFunctionBreakpointsResponse, args: DebugProtocol.SetFunctionBreakpointsArguments): Promise<void>;
    protected doSetFunctionBreakPointsRequest(response: DebugProtocol.SetFunctionBreakpointsResponse, args: DebugProtocol.SetFunctionBreakpointsArguments): Promise<void>;
    /**
     * Resolved which VS breakpoints needs to be installed, which
     * GDB breakpoints need to be deleted and which VS breakpoints
     * are already installed with which matching GDB breakpoint.
     * @param vsbps VS DAP breakpoints
     * @param gdbbps GDB breakpoints
     * @param matchFn matcher to compare VS and GDB breakpoints
     * @returns resolved -> array maintaining order of vsbps that identifies whether
     * VS breakpoint has a cooresponding GDB breakpoint (gdbbp field set) or needs to be
     * inserted (gdbbp field empty)
     * deletes -> GDB bps ids that should be deleted because they don't match vsbps
     */
    protected resolveBreakpoints<T>(vsbps: T[], gdbbps: mi.MIBreakpointInfo[], matchFn: (vsbp: T, gdbbp: mi.MIBreakpointInfo) => boolean): {
        resolved: Array<{
            vsbp: T;
            gdbbp?: mi.MIBreakpointInfo;
        }>;
        deletes: string[];
    };
    protected breakpointLocationsRequest(response: DebugProtocol.BreakpointLocationsResponse, args: DebugProtocol.BreakpointLocationsArguments): Promise<void>;
    protected configurationDoneRequest(response: DebugProtocol.ConfigurationDoneResponse, _args: DebugProtocol.ConfigurationDoneArguments): Promise<void>;
    protected convertThread(thread: mi.MIThreadInfo): ThreadWithStatus;
    protected threadsRequest(response: DebugProtocol.ThreadsResponse): Promise<void>;
    protected stackTraceRequest(response: DebugProtocol.StackTraceResponse, args: DebugProtocol.StackTraceArguments): Promise<void>;
    protected nextRequest(response: DebugProtocol.NextResponse, args: DebugProtocol.NextArguments): Promise<void>;
    protected stepInRequest(response: DebugProtocol.StepInResponse, args: DebugProtocol.StepInArguments): Promise<void>;
    protected stepOutRequest(response: DebugProtocol.StepOutResponse, args: DebugProtocol.StepOutArguments): Promise<void>;
    protected continueRequest(response: DebugProtocol.ContinueResponse, args: DebugProtocol.ContinueArguments): Promise<void>;
    protected pauseRequest(response: DebugProtocol.PauseResponse, args: DebugProtocol.PauseArguments): Promise<void>;
    protected scopesRequest(response: DebugProtocol.ScopesResponse, args: DebugProtocol.ScopesArguments): void;
    protected variablesRequest(response: DebugProtocol.VariablesResponse, args: DebugProtocol.VariablesArguments): Promise<void>;
    protected setVariableRequest(response: DebugProtocol.SetVariableResponse, args: DebugProtocol.SetVariableArguments): Promise<void>;
    protected setExpressionRequest(response: DebugProtocol.SetExpressionResponse, args: DebugProtocol.SetExpressionArguments): Promise<void>;
    /**
     * Send command to backend.
     * @param expression Command to be executed
     * @returns Promise for: - MI response as JS object if MI command, - `undefined` if CLI command
     */
    protected sendCommandToGdb(gdb: IGDBBackend, expression: string, frameRef: FrameReference | undefined): Promise<any>;
    /**
     * Send command to other backends.
     * @param expression Command to be executed
     */
    protected sendCommandToOtherGdbs(gdb: IGDBBackend, expression: string, frameRef: FrameReference | undefined): Promise<void>;
    /**
     * Evaluates a GDB command.
     * @param response Response object.
     * @param expression Command to be executed, expected without leading '>'.
     * @param frameRef Frame reference. If undefined, then it refers to the global scope.
     */
    protected evaluateRequestGdbCommand(response: DebugProtocol.EvaluateResponse, expression: string, frameRef: FrameReference | undefined): Promise<void>;
    private isInstructionBreakpoint;
    private getFrameContext;
    protected evaluateRequest(response: DebugProtocol.EvaluateResponse, args: DebugProtocol.EvaluateArguments): Promise<void>;
    private extractExpressionFormat;
    protected doEvaluateRequest(response: DebugProtocol.EvaluateResponse, args: DebugProtocol.EvaluateArguments, alwaysAllowCliCommand: boolean): Promise<void>;
    protected getChildElements(varobj: VarObjType, frameHandle: number): Promise<string>;
    protected convertValue(variable: DebugProtocol.Variable): string | number | boolean;
    /**
     * Implement the cdt-gdb-adapter/Memory request.
     */
    protected memoryRequest(response: MemoryResponse, args: any): Promise<void>;
    protected disassembleRequest(response: DebugProtocol.DisassembleResponse, args: CDTDisassembleArguments): Promise<void>;
    protected readMemoryRequest(response: DebugProtocol.ReadMemoryResponse, args: DebugProtocol.ReadMemoryArguments): Promise<void>;
    /**
     * Implement the memoryWrite request.
     */
    protected writeMemoryRequest(response: DebugProtocol.WriteMemoryResponse, args: DebugProtocol.WriteMemoryArguments): Promise<void>;
    protected disconnectRequest(response: DebugProtocol.DisconnectResponse, _args: DebugProtocol.DisconnectArguments): Promise<void>;
    protected sendStoppedEvent(reason: string, threadId: number, allThreadsStopped?: boolean): void;
    protected handleGDBStopped(result: any): void;
    protected sendContinuedEvent(threadId: number, allThreadsContinued?: boolean): void;
    protected handleGDBResume(result: any): void;
    private handleSilentPauseRequests;
    private updateIsRunning;
    protected handleGDBAsync(resultClass: string, resultData: any): void;
    private handleCmdParamChanged;
    protected handleGDBNotify(notifyClass: string, notifyData: any): void;
    protected handleGDBResult(notifyClass: string, notifyData: any): void;
    protected handleVariableRequestFrame(ref: FrameVariableReference): Promise<DebugProtocol.Variable[]>;
    protected handleVariableRequestObject(ref: ObjectVariableReference): Promise<DebugProtocol.Variable[]>;
    /** Query GDB using varXX name to get complete variable name */
    protected getFullPathExpression(inputVarName: string, gdb: IGDBBackend): Promise<string>;
    private registerMap;
    private registerMapReverse;
    protected handleVariableRequestRegister(ref: RegisterVariableReference): Promise<DebugProtocol.Variable[]>;
    protected getAddr(varobj: VarObjType, gdb: IGDBBackend): Promise<string>;
    protected isChildOfClass(child: mi.MIVarChild): boolean;
}
export {};
